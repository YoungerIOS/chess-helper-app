# 头像边框检测改进说明

## 问题分析

原有的`_detect_avatar_border`方法对JJ平台效果很好，但对TT平台经常误判，主要原因包括：

1. **绿色范围过于宽泛**：TT平台的绿色范围`[35, 50, 50]`到`[85, 255, 255]`太宽，容易误判头像中的绿色元素
2. **缺乏形状特征验证**：没有利用TT平台边框是正方形（有圆角）的特征
3. **阈值设置不合理**：面积阈值150可能对某些情况过于敏感
4. **缺乏边缘检测**：没有利用边框的几何特征

## 改进方案

### 1. 颜色范围优化

**TT平台颜色范围调整：**
- 绿色：从`[35, 50, 50]`缩小到`[40, 80, 80]`，减少误判
- 黄色：保持`[20, 100, 100]`到`[40, 255, 255]`
- 新增蓝色：`[100, 80, 80]`到`[130, 255, 255]`，覆盖TT平台后期可能显示的蓝色

**面积阈值调整：**
- 从150降低到100，提高检测灵敏度

### 2. 形状特征验证

新增`_validate_tt_contour`方法，专门验证TT平台的轮廓特征：

- **长宽比验证**：检查是否为正方形（0.8 ≤ 长宽比 ≤ 1.2）
- **矩形度验证**：检查轮廓与边界矩形的匹配度（> 0.6）
- **圆形度验证**：确保不是圆形（< 0.8）
- **实心度验证**：检查轮廓的凸性（> 0.7）
- **位置验证**：确保边框在头像边缘，不在中心区域
- **大小验证**：检查边框占据头像的合理比例

### 3. 多特征融合评分系统

采用评分制来判断轮廓的有效性：

- **形状特征**：长宽比、矩形度、圆形度、实心度（最高8分）
- **位置特征**：轮廓位置（1分）
- **大小特征**：面积比例（最高2分）
- **总分要求**：需要至少6分才能认为是有效的倒计时边框

### 4. 边缘检测辅助验证

新增`_validate_with_edge_detection`方法：

- 使用Canny边缘检测分析轮廓区域
- 计算边缘密度（边缘像素占总像素的比例）
- 边框应该有适中的边缘密度（0.05 ≤ 密度 ≤ 0.3）

### 5. 调试模式

新增调试功能：

- `set_debug_mode(enabled)`：启用/禁用调试模式
- 显示检测过程中的详细信息
- 包括颜色检测、轮廓验证、评分等

## 使用方法

### 启用调试模式

```python
# 在ChessCaptureManager实例上启用调试模式
manager = ChessCaptureManager(ui_message_queue)
manager.set_debug_mode(True)
```

### 运行测试

```bash
# 运行测试脚本验证改进效果
python test_avatar_detection.py
```

## 预期效果

1. **减少误判**：通过形状验证和颜色范围优化，大幅减少TT平台的误判
2. **提高准确率**：多特征融合评分系统提供更可靠的判断依据
3. **保持兼容性**：JJ平台的检测逻辑保持不变，确保现有功能不受影响
4. **便于调试**：调试模式帮助分析检测过程中的问题

## 技术细节

### 关键参数

- **长宽比阈值**：0.8-1.2（正方形）
- **矩形度阈值**：> 0.6
- **圆形度阈值**：< 0.8
- **实心度阈值**：> 0.7
- **边缘密度阈值**：0.05-0.3
- **评分阈值**：≥ 6分

### 算法流程

1. 颜色检测：HSV颜色空间检测绿色、黄色、蓝色
2. 轮廓提取：形态学操作后提取轮廓
3. 形状验证：计算几何特征并评分
4. 边缘检测：Canny边缘检测辅助验证
5. 综合判断：多特征融合评分决定最终结果

## 注意事项

1. 调试模式会增加控制台输出，生产环境建议关闭
2. 参数阈值可能需要根据实际使用情况微调
3. 边缘检测验证仅用于TT平台，不影响JJ平台
4. 改进后的方法向后兼容，不会破坏现有功能 